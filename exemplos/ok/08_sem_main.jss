// A função 'main' é opcional: um programa pode ter só declarações e funções.
const real TAXA = 0.05;

function real comJuros(real valor) {
  return valor + valor * TAXA;
}

function int dobro(int n) {
  return n * 2;
}
